# Examples

- `7676134473112489247.json` — a full live `videoData` record captured from
  TikTok's `embed/v2` SSR page, stored exactly as normalized by
  `tiktok_tools.metadata.normalize`. Useful as a fixture for consumers of the
  package output format.